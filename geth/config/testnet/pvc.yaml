apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geth-testnet-pvc
spec:
  storageClassName: ssd-retain
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany
  resources:
    requests:
      storage: 1000G
