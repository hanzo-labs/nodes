apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: geth-volume-claim
spec:
  storageClassName: geth-storage-class
  accessModes:
    - ReadWriteOnce
    - ReadOnlyMany
  resources:
    requests:
      storage: 200Gi
